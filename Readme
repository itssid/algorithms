C program to find sum of all divisors of a positive integer n.

Use the following command to compile
gcc sum_of_divisor.c -o sum_of_divisor -lm

Use the following command to run program 
./sum_of_divisor
